services:
  narratoai:
    build:
      context: .
      dockerfile: Dockerfile
    image: narratoai:latest
    container_name: narratoai
    ports:
      - "8866:8866"
    volumes:
      - type: bind
        source: ${WORKSPACE_ROOT:-../AIVideoGPT-workspace}
        target: /NarratoAI-workspace
      - ./config.toml:/NarratoAI/config.toml
      - ./resource:/NarratoAI/resource:ro

    environment:
      - PYTHONUNBUFFERED=1
      - TZ=Asia/Shanghai
      - APP_PORT=8866
      - NARRATO_WORKSPACE_ROOT=/NarratoAI-workspace
    restart: unless-stopped

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8866/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
